{
  "name": "@panda-ng/prisma",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "seed": "ts-node -e 'process.env.NODE_PATH=require.resolve(\".\")' seed.ts",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20.12.0",
    "prisma": "^5.13.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
